{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run PSP Homebrew with PPSSPP (Flatpak)",
      "type": "cppdbg",
      "request": "launch",
      "program": "/usr/bin/flatpak",
      "args": [
        "run",
        "org.ppsspp.PPSSPP",
        "${workspaceFolder}/build/EBOOT.PBP"
      ],
      "cwd": "${workspaceFolder}"
    }
  ]
}